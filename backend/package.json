{
  "name": "match-backend",
  "version": "1.1.0",
  "description": "精准匹配平台后端服务",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "backup": "node -e \"require('./database').backupDatabase()\""
  },
  "dependencies": {
    "better-sqlite3": "^12.10.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}